'''
** Hàm count():
- Đếm số phần tử chỉ định trong Tuple
- Cú pháp: Tên_Tuple.count(Phần tử)

** Hàm index():
- Trả về một số (chỉ mục) chỉ vị trí đầu
tiên của phần tử chỉ định được phát hiện trong
Tuple
- Cú pháp : Tên_Tuple.index(Phần tử)

** Hàm all()
- Trả về giá trị True nếu tất cả các phần tử
của Tupple không chứa giá trị False hay Tuple rỗng
- Cú pháp: all(Tên_Tuple)

** Hàm any():
- Trả về giá trị True nếu có bất kỳ phần tử của Tupple
chứa giá trị True, trả về False khi toàn bộ Tupple chứa 
giá trị False hay Tuple rỗng

- Cú pháp : any(Tên_Tuple)

** Hàm enumerate():
- Thêm vào một bộ đếm vào trước mỗi thành phần
có thể lặp (Iterable) cặp index và trả về kết quả
dưới dạng đối tượng liệt kê (enumerate object)

- Cú pháp: enumerate(Tên_thành_Phần [, Số_Bắt_Đầu_Bộ Đếm])

- Trong đó:
+ Tên_thành_Phần: có thể là một List, Chuỗi hay Tuple
+ Số_Bắt_Đầu_Bộ Đếm: nếu ko ghi sẽ bắt đầu từ 0

** Hàm len():
- Trả về chiều dài (số phần tử) của Tupple
- Cú pháp : len(Tên_tupple)

** Hàm max():
- Trả về phần tử lớn nhất của Tupple ( có cùng kiểu dữ liệu)
- Cú pháp: max(Tên_Tupple)

** Hàm min():
- Trả về phần tử nhỏ nhất của Tupple ( có cách kiểu dữ liệu)
- Cú pháp: min(Tên_Tupple)

** hàm sorted():
- Lấy các phần tử trong tupple và tạo ra 
một list mới được sắp xếp ( tupple ko sắp xếp được)
- Cú pháp : sorted(Tên_Tupple)

** Hàm sum():
- Trả về tổng tất cả các phần tử trong tupple
- Cú pháp : sum(Tên_Tupple)

** Hàm tuple():
- Chuyển đổi những đối tượng có thể lặp
(Iterable) như List, String, Set, Dictionary sang Tuple

- Cú pháp : tuple(Iterable)
+ Nếu ko có tham số truyền vào sẽ trả về một
tuple rỗng


'''